release:
    name: my-release
    namespace: crossplane-system
suite: Compositions
templates:
    -   templates/compositions.yaml
tests:
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "range \\$retainedDataPlaneKey, \\$_ := \\$retainedDataPlanes \\}\\}\\s+\\{\\{- if not \\(has \\$retainedDataPlaneKey \\$renderedDataPlaneKeys\\) \\}\\}\\s+\\{\\{- \\$retainedBackupInstanceName := index \\$dataPlaneNames \\$retainedDataPlaneKey \"backupInstance\" \\}\\}\\s+apiVersion: dataprotection[^\\n]+\\s+kind: BackupInstanceBlobStorage"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "crossplane.io/external-name: \\{\\{ \\$retainedBackupInstanceName \\}\\}\\s+gotemplating[^:]*: liferay-backup-instance-retained-\\{\\{ \\$retainedDataPlaneKey \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "retainedDataPlane: \\{\\{ \\$retainedDataPlaneKey \\}\\}\\s+name: \\{\\{ \\$retainedBackupInstanceName \\}\\}-retained\\s+spec:\\s+forProvider:\\s+vaultId: [^\\n]+backupVaults/\\{\\{ \\$backupVaultName \\}\\}\\s+managementPolicies: \\{\\{ list \"Delete\" \"Observe\" \\| toJson \\}\\}"
        it: Adopts each retained backup instance without ever updating it
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "range \\$retainedDataPlaneKey, \\$_ := \\$retainedDataPlanes \\}\\}\\s+\\{\\{- if not \\(has \\$retainedDataPlaneKey \\$renderedDataPlaneKeys\\) \\}\\}\\s+\\{\\{- \\$retainedDatabaseServerName := index \\$dataPlaneNames \\$retainedDataPlaneKey \"databaseServer\" \\}\\}\\s+---\\s+apiVersion: dbforpostgresql[^\\n]+\\s+kind: FlexibleServer"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "crossplane.io/external-name: \\{\\{ \\$retainedDatabaseServerName \\}\\}\\s+gotemplating[^:]*: database-retained-\\{\\{ \\$retainedDataPlaneKey \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "retainedDataPlane: \\{\\{ \\$retainedDataPlaneKey \\}\\}\\s+name: \\{\\{ \\$retainedDatabaseServerName \\}\\}-retained\\s+spec:\\s+forProvider:\\s+resourceGroupName: \\{\\{ \\$env.resourceGroupName \\}\\}\\s+managementPolicies: \\{\\{ list \"Delete\" \"Observe\" \\| toJson \\}\\}"
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: retained-until
        it: Adopts each retained server by name alone without managing its spec
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "composition-resource-name: backup-restore-role-binding"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "name: \\{\\{ \\$baseName \\}\\}-backup-restore-role-binding"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: ClusterRole\\s+name: backup-restore-cluster-role"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: ServiceAccount\\s+name: backup-restore-service-account\\s+namespace: \\{\\{ \\$namespace \\}\\}"
        it: Binds the restore service account to the restore cluster role
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: Job"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "image: postgres:18-alpine"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "image: mcr.microsoft.com/azure-cli"
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: postgresql.sql.m.crossplane.io
        it: Bootstraps the database through a job rather than the SQL provider
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "composition-resource-name: backup-restore-service-account"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: \$backupRestoreIdentityClientId := dig "liferay-backup-restore-identity" "resource" "status" "atProvider" "clientId"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: \{\{- if \$backupRestoreIdentityClientId \}\}
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "azure.workload.identity/client-id: \\{\\{ \\$backupRestoreIdentityClientId \\}\\}"
        it: Creates the restore service account with its workload identity client id
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "composition-resource-name: liferay-backup-restore-identity\\s"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "composition-resource-name: liferay-backup-restore-identity-federation"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "parentIdRef:\\s+name: \\{\\{ \\$backupRestoreIdentityName \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "subject: system:serviceaccount:\\{\\{ \\$namespace \\}\\}:backup-restore-service-account"
        it: Federates the restore identity to the restore workflow service account
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: CREATE ROLE {{ \$databaseOwnerRole }} NOLOGIN
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: GRANT CONNECT, TEMPORARY ON DATABASE
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: SET ROLE {{ \$databaseOwnerRole }}
        it: Grants the application a least privileged database role
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "composition-resource-name: liferay-backup-restore-storage-\\{\\{ \\$dataPlane \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "name: \\{\\{ \\$backupRestoreIdentityName \\}\\}-storage-\\{\\{ \\$dataPlane \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "principalId: \\{\\{ \\$backupRestoreIdentityPrincipalId \\}\\}\\s+principalType: ServicePrincipal\\s+roleDefinitionName: Storage Blob Data Contributor\\s+scope: [^\\n]+Microsoft.Storage/storageAccounts/\\{\\{ index \\$storageAccountNames \\$dataPlane \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "composition-resource-name: liferay-backup-restore-storage-\\{\\{ \\$dataPlane \\}\\}[\\s\\S]{0,900}?name: iam\\s+---\\s+apiVersion[\\s\\S]{0,300}?liferay-backup-restore-storage-reader"
        it: Grants the restore identity blob access on each plane storage account
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "composition-resource-name: liferay-backup-restore-database-\\{\\{ \\$dataPlane \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "principalId: \\{\\{ \\$backupRestoreIdentityPrincipalId \\}\\}\\s+principalType: ServicePrincipal\\s+roleDefinitionName: Reader\\s+scope: [^\\n]+Microsoft.DBforPostgreSQL/flexibleServers/"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "Microsoft.DBforPostgreSQL/flexibleServers/\\{\\{ index \\$databaseServerNames \\$dataPlane \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "range \\$dataPlane := \\$dataPlanes \\}\\}\\s+apiVersion: authorization[^\\v]*\\s+kind: RoleAssignment\\s+metadata:\\s+annotations:\\s+gotemplating[^:]*: liferay-backup-restore-database-"
        it: Grants the restore identity read on each plane database server
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "composition-resource-name: liferay-backup-restore-storage-reader-\\{\\{ \\$dataPlane \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "principalId: \\{\\{ \\$backupRestoreIdentityPrincipalId \\}\\}\\s+principalType: ServicePrincipal\\s+roleDefinitionName: Reader\\s+scope: [^\\n]+Microsoft.Storage/storageAccounts/"
        it: Grants the restore identity read on each plane storage account
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "composition-resource-name: liferay-backup-restore-database-retained-\\{\\{ \\$retainedDataPlaneKey \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "roleDefinitionName: Reader\\s+scope: [^\\n]+Microsoft.DBforPostgreSQL/flexibleServers/\\{\\{ index \\$dataPlaneNames \\$retainedDataPlaneKey \"databaseServer\" \\}\\}"
        it: Grants the restore identity read on each retained database server
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: \{\{- if \$backupRestoreIdentityPrincipalId \}\}
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: dig "liferay-backup-restore-identity" "resource" "status" "atProvider" "principalId"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "composition-resource-name: liferay-backup-restore-vault-backup-contributor"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "roleDefinitionName: Backup Contributor"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "Microsoft.DataProtection/backupVaults/\\{\\{ \\$backupVaultName \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "composition-resource-name: liferay-backup-restore-vault-reader"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "principalId: \\{\\{ \\$backupRestoreIdentityPrincipalId \\}\\}\\s+principalType: ServicePrincipal\\s+roleDefinitionName: Reader\\s+scope: [^\\n]+Microsoft.DataProtection/backupVaults/\\{\\{ \\$backupVaultName \\}\\}"
        it: Grants the restore identity the vault roles the restore and the protection switch demand
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "GRANT USAGE, CREATE ON SCHEMA public TO \\{\\{ \\$databaseOwnerRole \\}\\};"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "--dbname \"\\{\\{ \\$databaseName \\}\\}\""
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: --dbname postgres
        it: Grants the shared owner role create rights on the public schema
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "principalId: \\{\\{ \\$backupVaultPrincipalId \\}\\}\\s+principalType: ServicePrincipal\\s+roleDefinitionName: Storage Account Backup Contributor"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: \{\{- if \$backupVaultPrincipalId \}\}
        it: Grants the vault identity only the role the document library demands
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: \{\{- if \$backupRoleId \}\}
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: \$backupRoleId := dig \(printf "liferay-backup-vault-storage-%s" \$dataPlane\) "resource" "status" "atProvider" "id" ""
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "resolve: Always"
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: \$areVaultRolesAssigned
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: \$backupRoleAssignmentNames
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: \$isBackupRoleAssigned
        it: Holds each backup instance back until its own vault role assignment exists
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\$backupInstanceDataPlanes := list \\$activeDataPlane"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "range \\$dataPlane := \\$backupInstanceDataPlanes \\}\\}\\s+\\{\\{- \\$backupRoleId := dig"
        it: Holds protection off the restore target until it is promoted
    -   asserts:
            -   equal:
                    path: spec.compositeTypeRef.apiVersion
                    value: liferay.com/v1alpha1
            -   equal:
                    path: spec.pipeline[0].functionRef.name
                    value: function-environment-configs
            -   equal:
                    path: spec.pipeline[1].functionRef.name
                    value: function-go-templating
            -   equal:
                    path: spec.pipeline[2].functionRef.name
                    value: function-auto-ready
            -   hasDocuments:
                    count: 2
            -   isKind:
                    of: Composition
            -   documentIndex: 0
                equal:
                    path: metadata.labels["liferay.com/interface"]
                    value: liferay-infrastructure
            -   documentIndex: 0
                equal:
                    path: metadata.name
                    value: liferay-infrastructure
            -   documentIndex: 0
                equal:
                    path: spec.compositeTypeRef.kind
                    value: LiferayInfrastructure
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: FlexibleServer"
            -   documentIndex: 1
                equal:
                    path: metadata.labels["liferay.com/interface"]
                    value: liferay-overlay
            -   documentIndex: 1
                equal:
                    path: metadata.name
                    value: liferay-overlay
            -   documentIndex: 1
                equal:
                    path: spec.compositeTypeRef.kind
                    value: LiferayOverlay
            -   documentIndex: 1
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: Account"
        it: Implements the shared Liferay APIs through the packaged function pipeline
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\{\\{- if or \\(eq \\$dataPlane \\$activeDataPlane\\) \\(eq \\$restorePhase \"promoting\"\\) \\}\\}\\s+---\\s+apiVersion: storage.azure.m.upbound.io/v1beta1\\s+kind: Container"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "writeConnectionSecretToRef:\\s+name: liferay-storage-\\{\\{ \\$dataPlane \\}\\}-credentials\\s+\\{\\{- if or \\(eq \\$dataPlane \\$activeDataPlane\\)"
        it: Keeps the demoted document library container until the restore is cleaned up
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\$observedDatabaseServer := dig \\(printf \"database-primary-%s\" \\$dataPlane\\) \"resource\" dict \\$observedResources"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "if and \\$observedDatabaseServer \\(not \\(dig \"metadata\" \"deletionTimestamp\" \"\" \\$observedDatabaseServer\\)\\) -\\}\\}\\s+\\{\\{- \\$_ := set \\$dataPlaneKeys \\$dataPlane \\(dig \"metadata\" \"labels\" \"dataPlaneKey\" \\$dataPlane \\$observedDatabaseServer\\)"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: FlexibleServer\\s+metadata:[\\s\\S]{0,300}?dataPlaneKey: \\{\\{ index \\$dataPlaneKeys \\$dataPlane \\}\\}\\s+name: \\{\\{ \\$databaseServerName \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: Account\\s+metadata:[\\s\\S]{0,300}?dataPlaneKey: \\{\\{ index \\$dataPlaneKeys \\$dataPlane \\}\\}\\s+name: \\{\\{ \\$storageAccountName \\}\\}"
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: observedStorageAccount|observedBackupInstance
        it: Latches each plane key from the observed server unless it is being released
    -   asserts:
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: BackupPolicyPostgreSQLFlexibleServer"
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: BackupInstancePostgreSQLFlexibleServer"
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: Long Term Retention Backup Role
        it: Leaves the database to its own automatic backups rather than the vault
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "softDelete: \\{\\{ \\$forceDestroy \\| ternary \"Off\" \"On\" \\| quote \\}\\}"
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "softDelete: \"On\""
        it: Lets forceDestroy disable vault soft delete so the vault can be destroyed
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "autoGeneratePassword: true"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "name: database-admin-password"
        it: Lets the provider generate and own the database administrator password
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: FlexibleServerActiveDirectoryAdministrator"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "principalType: ServicePrincipal"
        it: Makes the bootstrap identity a Microsoft Entra administrator
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: \$backupScheduleHash := printf "%s\|%s\|%s" \$backupRepeatingTimeInterval
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: sha256sum \| trunc 6
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "composition-resource-name: liferay-backup-policy-storage-\\{\\{ \\$backupScheduleHash \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: printf "%s-storage-policy-%s" \$backupVaultName \$backupScheduleHash
        it: Names each policy after its schedule so a schedule change rolls the policy
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "set \\$resourceNames \"backupInstance\" \\(printf \"%s-storage-instance-%s\" \\$backupVaultName \\$dataPlane\\)"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "set \\$resourceNames \"backupInstance\" \\(printf \"%s-%s\" \\$backupVaultName \\$resourceNames.storageAccount\\)"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: BackupInstanceBlobStorage\\s+metadata:[\\s\\S]{0,300}?dataPlaneKey: \\{\\{ index \\$dataPlaneKeys \\$dataPlane \\}\\}\\s+name: \\{\\{ index \\$dataPlaneNames \\(index \\$dataPlaneKeys \\$dataPlane\\) \"backupInstance\" \\}\\}"
        it: Names each restored backup instance after its storage account and keeps the original name on a plain key
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "else if and \\(eq \\$restorePhase \"provisioning\"\\) \\(eq \\$dataPlane \\$targetActiveDataPlane\\) \\$restoreGeneration -\\}\\}\\s+\\{\\{- \\$_ := set \\$dataPlaneKeys \\$dataPlane \\(printf \"%s-%s\" \\$dataPlane \\$restoreGeneration\\)"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "else -\\}\\}\\s+\\{\\{- \\$_ := set \\$dataPlaneKeys \\$dataPlane \\$dataPlane -\\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\$resourceNames := dict \"databaseServer\" \\(printf \"%s-%s\" \\$baseName \\$dataPlaneKey\\)"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\$databaseServerName := index \\$databaseServerNames \\$dataPlane"
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "restorePointInTime \\| default \"\" \\| sha256sum"
        it: Names each restored server after its restore generation so no two restores share one
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "set \\$resourceNames \"storageAccount\" \\(printf \"%.11s%s%s%s\" \\(\\$projectIdFull \\| replace \"-\" \"\" \\| lower\\) \\$uidHash \\(trunc 1 \\$dataPlane\\) \\$generation\\)"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "set \\$resourceNames \"storageAccount\" \\(printf \"%s%s\" \\$storageAccountBase \\(trunc 1 \\$dataPlane\\)\\)"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "LIFERAY_DOCLIB_STORAGE_ACCOUNT_NAME: \\{\\{ index \\$storageAccountNames \\$activeDataPlane \\| b64enc \\| quote \\}\\}"
        it: Names each restored storage account after its restore generation so the demoted account keeps its own
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\{\\{- \\$isDatabaseRestoreTarget := and \\(eq \\$restorePhase \"provisioning\"\\) \\(eq \\$dataPlane \\$targetActiveDataPlane\\) \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\{\\{- if not \\$isDatabaseRestoreTarget \\}\\}\\s+administratorLogin: liferay_database_admin\\s+administratorPasswordSecretRef:"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\{\\{- if not \\$isDatabaseRestoreTarget \\}\\}\\s+autoGeneratePassword: true"
        it: Omits the administrator credentials from the restored plane
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "composition-resource-name: usage-storage-account-by-backup-instance-\\{\\{ \\$dataPlane \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "by:\\s+apiVersion: dataprotection.azure.m.upbound.io/v1beta1\\s+kind: BackupInstanceBlobStorage"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "of:\\s+apiVersion: storage.azure.m.upbound.io/v1beta1\\s+kind: Account"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "range \\$dataPlane := \\$backupInstanceDataPlanes \\}\\}\\s+\\{\\{- if dig \\(printf \"liferay-backup-vault-storage-%s\" \\$dataPlane\\)"
        it: Orders the storage account deletion behind its backup instance
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "if \\$demotedDataPlane -\\}\\}\\s+\\{\\{- \\$backupInstanceDataPlanes = append \\$backupInstanceDataPlanes \\$demotedDataPlane"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "vaultIdRef:\\s+name: \\{\\{ \\$backupVaultName \\}\\}\\s+managementPolicies: \\{\\{ \\(eq \\$dataPlane \\$demotedDataPlane\\) \\| ternary \\$demotedManagementPolicies \\$managementPolicies \\}\\}"
        it: Orphans the demoted backup instance only when the workflow has marked its data plane retained
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "if and \\(eq \\$restorePhase \"promoting\"\\) \\(hasKey \\$retainedDataPlanes \\(index \\$dataPlaneKeys \\$targetInactiveDataPlane\\)\\) -\\}\\}\\s+\\{\\{- \\$demotedDataPlane = \\$targetInactiveDataPlane -\\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\$demotedManagementPolicies := \\(eq \\$demotedDataPlane \"\"\\) \\| ternary \\$managementPolicies \\(list \"Create\" \"Observe\" \"Update\" \\| toJson\\)"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: FlexibleServerActiveDirectoryAdministrator[\\s\\S]*?managementPolicies: \\{\\{ \\(eq \\$dataPlane \\$demotedDataPlane\\) \\| ternary \\$demotedManagementPolicies \\$managementPolicies \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "managementPolicies: \\{\\{ \\$isDemotedDataPlane \\| ternary \\$demotedManagementPolicies \\$managementPolicies \\}\\}"
        it: Orphans the demoted server only when the workflow has marked its data plane retained
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: BackupPolicyBlobStorage"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: BackupInstanceBlobStorage"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "vaultDefaultRetentionDuration:"
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: operationalDefaultRetentionDuration
        it: Protects the document library with a vaulted policy so restore can cross accounts
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: \{\{- if \$backup.enabled \}\}
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: BackupVault"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "datastoreType: VaultStore"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "type: SystemAssigned"
        it: Provisions the backup vault only once backups are enabled
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: ConfigMap"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "name: database-server-details"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: DATABASE_ENDPOINT
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: DATABASE_PORT
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: DATABASE_USERNAME
        it: Publishes only the database endpoint it discovered from Azure
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: LIFERAY_OVERLAY_BACKEND_OPTIONS.*account=
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: LIFERAY_OVERLAY_BUCKET_NAME
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: LIFERAY_OVERLAY_CONTAINER_NAME
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: RCLONE_AZUREBLOB_ACCOUNT
        it: Publishes the overlay bucket and its backend options
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "sharedAccessKeyEnabled: true"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "name: liferay-storage-\\{\\{ \\$dataPlane \\}\\}-credentials"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: attribute.primary_connection_string
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\| toString \\| b64dec"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: LIFERAY_DOCLIB_CONNECTION_STRING
        it: Publishes the storage account connection string for the document library store
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: \$backupVaultIdentity.principalId
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: index \$backupVaultIdentity
        it: Reads the vault principal from the identity object rather than indexing it
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "gotemplating[^:]*: backup-restore-release-cron-workflow"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "kind: CronWorkflow\\s+metadata:\\s+labels:\\s+\\{\\{- \\$tags \\| toYaml \\| nindent 20 \\}\\}\\s+name: backup-restore-release-cron-workflow\\s+namespace: \\{\\{ \\$namespace \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "concurrencyPolicy: Forbid\\s+schedules:\\s+-   0 \\* \\* \\* \\*"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "clusterScope: true\\s+name: backup-restore-cluster-workflow-template\\s+template: release-retained-data-planes"
        it: Releases retained data planes hourly from a CronWorkflow the composition owns
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "range \\$i, \\$dataPlane := \\$dataPlanes \\}\\}\\s+\\{\\{- \\$storageAccountName := index \\$storageAccountNames \\$dataPlane \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "range \\$dataPlane := \\$dataPlanes \\}\\}\\s+apiVersion: authorization[^\\v]*\\s+kind: RoleAssignment\\s+metadata:\\s+annotations:\\s+gotemplating[^:]*: liferay-storage-role-assignment-"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "range \\$dataPlane := \\$dataPlanes \\}\\}\\s+apiVersion: authorization[^\\v]*\\s+kind: RoleAssignment\\s+metadata:\\s+annotations:\\s+gotemplating[^:]*: liferay-backup-vault-storage-"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "range \\$dataPlane := \\$dataPlanes \\}\\}\\s+apiVersion: authorization[^\\v]*\\s+kind: RoleAssignment\\s+metadata:\\s+annotations:\\s+gotemplating[^:]*: liferay-backup-restore-storage-"
        it: Renders each storage account and its role assignments only while its plane is rendered
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: R/%sT%s:00\+00:00/%s
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\"Monday\" \"2024-01-01\""
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: \$isWeekly \| ternary "P1W" "P1D"
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "schedule: cron"
        it: Renders the schedule as an ISO 8601 repeating interval rather than a cron expression
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\$isDatabaseReady := and \\(ne \\$activeDatabaseFqdn \"\"\\) \\$isDatabaseBootstrapped"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "managedServiceDetailsReady: \\{\\{ and \\$isDatabaseReady \\$isDoclibReady \\}\\}"
        it: Reports the managed service details ready only once every service is ready
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\{\\{- if \\$isDatabaseRestoreTarget \\}\\}\\s+initProvider:\\s+createMode: PointInTimeRestore"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "pointInTimeRestoreTimeInUtc: \\{\\{ \\$databaseSpec.restorePointInTime \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\$restoreSourceDatabaseServerName := dig \\(\\$databaseSpec.restoreSourceDataPlane \\| default \\(index \\$dataPlaneKeys \\$activeDataPlane\\)\\) \"databaseServer\" \"\" \\$dataPlaneNames"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "sourceServerId: /subscriptions/[^\\n]+/flexibleServers/\\{\\{ \\$restoreSourceDatabaseServerName \\}\\}"
            -   documentIndex: 0
                notMatchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: sourceServerIdSelector
        it: Restores the inactive plane to a point in time through initProvider
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "blobProperties:\\s+changeFeedEnabled: true"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "versioningEnabled:"
        it: Retains the change feed and versions that vaulted backup replication requires
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\$backupRetentionDays := max 7 \\(min 35 \\(int \\(\\$backup.retentionDays \\| default 30\\)\\)\\)"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "backupRetentionDays: \\{\\{ \\$backupRetentionDays \\}\\}"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\$backupRetentionDuration := printf \"P%dD\" \\$backupRetentionDays"
        it: Retains the document library and the database for the same clamped period
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "storageAccountContainerNames:\\s+-\\s+document-library"
        it: Scopes the document library protection to its own container
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "activeDirectoryAuthEnabled: true"
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: resource-type oss-rdbms
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: PGPASSWORD=\$\(cat /entra/token\)
        it: Uses Microsoft Entra for the bootstrap connection rather than a password
    -   asserts:
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: \{\{- if \$isDoclibReady \}\}
            -   documentIndex: 0
                matchRegex:
                    path: spec.pipeline[1].input.inline.template
                    pattern: "\\$isDoclibReady := and \\(ne \\$doclibConnectionString \"\"\\) \\$isDoclibContainerReady"
        it: Withholds the document library details until the container and its credential exist